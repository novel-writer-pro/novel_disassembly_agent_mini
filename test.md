



#1. 防误报建设
  - 线上攻击日志关于注入类型的分析, 发送给大模型判断是否是恶意的,这里用到了两级漏斗(ｂert+ 微调后的Hunyuan7b)
    - agent的作用是介入了人工的闭环操作, 确认操作(比如 大模型也不置信的情况下,这里的LLM输出是0/1的softmax概率, 所以有百分数; 另外一个方面是需要运营加白还是推送给客户的决策动作)

#2. 漏报Agent
  - 根据外部的情报源,前场/客户/SRC反馈的问题, 传递给Agent, 分析应该怎么防御, 自己搭建一个简单的验证环境(漏洞环境和WAF的规则环境)  -> 编写规则进行防御,验证规则是有效的(这里还加载我们自己的黑白样本集) -> 可以防御,并且无误报 -> 通过Api发送到生产的引擎旁路验证 -> 周期性读取线上误报情况,同上防误报Agent -> 周期推送误报情况到企业微信机器人, webhook点击处置动作,决定是否发送到生产环境- 闭环.

#3. RAG问答助手Agent
  - 通过官网,工单(验证集),FAQ, iwiki等资源的来源做evidence->fact的绑定,构建图谱问答系统， embedding+bm25+UIE多路召回的问题,重排需蛇功能成问答, 这个是服务号的形式.

#4. 传统agent上,(自动化安全渗透工具)我自己之前使用 sougou/workflow做过启发式扫描器(基于DAG task), 就是Burpsuit发送到我们的mitmproxy -> 进行sql/xss/rfi等的注入攻击测试-> 人工验证等,现在大模型也可以做functionCall可以简化一些人工的步骤. 这个我业余也在做,但是还不成熟.
