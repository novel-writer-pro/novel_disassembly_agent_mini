# apps/web

独立前端目录。

当前已经提供一个无依赖静态原型：

```bash
cd apps/web
python3 -m http.server 4173
```

然后打开：

`http://127.0.0.1:4173`

用于演示：
- 导入小说入口
- pipeline profile 切换
- run / branch snapshot 展示
- recovery 动作矩阵
- export 返回形态
