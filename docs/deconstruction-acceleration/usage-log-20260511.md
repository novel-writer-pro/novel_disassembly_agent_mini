# 拆书加速使用日志（2026-05-11）

## 本次执行命令

1. 读取 worker inbox / task / mailbox，claim task 6
2. 仓内定位：
   - `docs/deconstruction-acceleration/*`
   - `tests/test_context_service.py`
   - `novel_analyzer/services/context_service.py`
3. 回归验证：
   - `pytest -q tests/test_context_service.py`
   - `pytest -q tests/test_context_bundle_cli.py tests/test_imitation_harness_service.py tests/test_whole_book_imitation_service.py`
4. 基线 benchmark：
   - 运行 `benchmark-baseline-20260511.md` 中的 Python 命令

## 观察

- 当前仓内尚未落地 `_deconstruction_profile` 真实运行时读写
- 因此本次 lane 以“保护默认 canonical 行为 + 建立 benchmark 零点 + 同步文档/日志”为主
- 这满足“不影响当前仿写默认行为”的硬约束

- 补充 `docs/deconstruction-acceleration/user-manual.md`，用于当前版本拆书加速优化的实际使用说明。

- 真实 20 章试跑：前 3 章成功，`show-run-status` 显示 chapter 4 失败于 `fact_extractor`，失败原因为 `job stalled for more than 180 seconds`。

- 长跑追踪更新：前 8 章成功，第 9 章失败于 `evidence_binder`，错误为 `job stalled for more than 180 seconds`。

- 为减少真实 DeepSeek 长跑中 staged 中间阶段被过早判定 stalled，已把 `chapter_job_stall_timeout_seconds` 默认值上调到 600，并补了对应 run_service 单测。

- 修复：已完成章节若已有 active canonical artifact，`retry-chapter` 现在会直接拒绝，避免 chapter job 与 artifact 进度错位。

- 修复：默认 stall timeout 已从 180 提高到 600，用于缓解真实 DeepSeek 长阶段调用过早被判 stalled。

- QA 精化补丁验证：
  - `python -m pytest -q tests/test_qa_service.py` → `6 passed`
  - `python -m pytest -q tests/test_run_service.py tests/test_cli_retry_bulk.py tests/test_application_layer.py -k "retry_failed_jobs_cli_skips_completed_chapters_with_artifacts or retry_refused_when_readable_artifact_already_exists or stalled_jobs_respects_timeout_setting"` → `3 passed, 21 deselected`
- 当前 focus 已切换：后续真实拆书验证忽略《万相之王》，改用“卫图”样例链路。

- 卫图真实拆书验证（独立库）：
  - 数据库：`novel_analyzer_weitu_deconstruction_20260511`
  - run：`754b205f-22fc-4eeb-962d-6d6800c4052d`
  - branch：`03c657c8-5389-4e42-9234-b14137c04125`
  - 输入：`/home/user/txt111/chapters` 前 20 章拼接成 `.tmp/weitu_first20.txt`
  - 模型：`deepseek-v4-flash @ https://api.deepseek.com/v1`
- 已验证通过：
  - `show-run-status`
  - `show-chapter`
  - `show-context`
  - `show-window`（1-5）
  - `search-branch`
  - `ask-branch`
- 真实吞吐观察：
  - 前 5 章完成时约耗时 31 分钟
  - 粗略均值约 `6.2 min/章`
  - 当前判断瓶颈集中在串行 LLM stages，而非 DB / 调度 / 恢复。

- 运行中期进度更新：
  - 当前已完成 7 章，第 8 章进入后段 stage。
  - 第 6 章出现过 `small_model_pipeline` JSON 失败（`model response does not contain valid JSON after repair`），但随后进入 `monolithic_fallback` 并最终完成，说明当前恢复/兜底链路在真实样例上确实生效。

- Ralph 性能优化第 1 刀：
  - quick 主链默认将 `writer_learning_lens` 从同步 stage 改为 deferred，占位保留 `writer_learning_notes=[]` 与 `_deconstruction_profile.writer_lens_status=deferred`。
  - 定向回归：`tests/test_analysis_service.py tests/test_run_service.py tests/test_context_service.py tests/test_qa_service.py -k "writer_lens_status or deferred_writer or build_deconstruction_profile or materialization_failure_restores_previous_active_artifact_and_blocks_job or stage_failure_falls_back_to_monolithic_and_persists_result or context_bundle_ignores_newer_shadow_window or branch_qa"` → `8 passed`。
  - 独立 smoke 库验证：provider 余额不足时仍可走 fallback 完成 1 章，说明本次改动未破坏 fallback 主线。

- Ralph 性能优化第 2 刀：
  - quick 主链默认将 `risk_aggregation` 从同步尾部工作改为 deferred event，不再阻塞章节完成后返回下一章推进。
  - 定向/扩展回归：`11 passed`。
- 卫图真实 run 最终状态：
  - `completed_chapters=20`
  - `failed_jobs=0`
  - `running_jobs=0`
  - `window_count=4`
  - `graph_node_count=348`
  - `graph_edge_count=7916`

- Ralph 性能优化第 3 刀：
  - `analysis_generator` 与 `anti_fabrication_guard` 不再携带完整 `graph_context_json`，改为只携带 compact `state_summary_json`（关键字段最多 3 项）。
  - 扩展回归：`13 passed`。
- 真实 20 章 branch QA 验证：
  - 当前 provider 因 `402 Insufficient Balance` 触发 QA 降级；
  - 但 `ask-branch` 仍返回保守答案、used_chapters、evidence、reasoning_path、graph_signal，说明消费链在 provider 异常时仍可降级可用。

- Ralph 性能优化第 4 刀：
  - `fact_extractor` 不再携带完整 `graph_context_json`，只消费 compact `state_summary_json`。
  - `evidence_binder` 不再携带 graph/state/window 上下文，只保留 `cleaned_text + fact_json`。
  - 扩展回归：`14 passed`。

- Ralph 性能优化第 5 刀：
  - `prior_context_json` 改为 compact 版，只保留小规模前情摘要与事实关键信息。
  - 扩展回归：`16 passed`。
- 最新 prompt 体积量化（卫图第 20 章样本）：
  - `fact_extractor`: `26099 -> 2455`（约 `-90.6%`）
  - `analysis_generator`: `23642 -> 1488`（约 `-93.7%`）
  - `anti_fabrication_guard`: `23101 -> 947`（约 `-95.9%`）

- 性能防回退护栏：
  - 新增基于卫图第 20 章真实上下文的 prompt budget tests。
  - 护栏包括：绝对长度上限 + 相对旧版缩减比例断言。
  - 扩展回归：`18 passed`。

- Ralph 性能优化第 6 刀：
  - `previous_summary` 输入统一截断到 compact 版（默认 220 chars）。
  - 扩展回归：`20 passed`。

- 运行观测埋点增强：
  - `record_raw_output.invocation_metadata` 新增 `prompt_char_counts` 与 `total_prompt_chars`。
  - 扩展回归：`22 passed`。
  - 这使得后续 funded-provider 真实 benchmark 可以直接从数据库读取每章 prompt 体积，而不必再临时脚本推算。

- benchmark 工具链验证：
  - 新增 `scripts/benchmark_deconstruction_run.py`
  - 已对卫图真实 20 章 run 执行一次汇总，得到：
    - `elapsed_seconds=4728.32721`
    - `avg_seconds_per_completed_chapter=236.4163605`
  - 当前这份旧 run 发生在 prompt-metrics 埋点之前，因此 `prompt_char_totals` 为空，符合预期。

- benchmark CLI 测试补齐：
  - `tests/test_benchmark_deconstruction_run.py` 已覆盖：
    - 有 prompt metrics 的 run 汇总
    - 无 prompt metrics 的旧 run 汇总
  - 扩展回归：`24 passed`。

- benchmark compare CLI：
  - 新增 `scripts/compare_deconstruction_benchmarks.py`
  - 可直接比较 baseline/candidate 两份 benchmark JSON 的：
    - `elapsed_seconds`
    - `avg_seconds_per_completed_chapter`
    - `failed_jobs`
    - `prompt_char_totals`
  - 扩展回归：`26 passed`。
