# 模块化与可维护代码开发要求

请遵守以下规范编写代码，确保项目结构清晰、文件解耦、易于定位和修改。

## 一、模块化与文件拆分

1. **单一职责**：每个文件、类、函数只负责一个明确的功能或业务逻辑。
2. **按功能拆分**：不同功能必须放在不同的文件中。禁止把所有代码（如路由、数据库操作、业务逻辑、UI 渲染）堆积在同一个文件内。
3. **文件规模控制**：单个源代码文件不超过 300 行（不含空行和注释）。超过时必须拆分出子模块或辅助文件。
4. **目录组织**：按业务模块、技术分层或特性（Feature）创建子目录。示例结构：
```
src/
├── modules/ # 业务模块
│ ├── user/
│ │ ├── controller.py
│ │ ├── service.py
│ │ ├── repository.py
│ │ └── models.py
│ └── order/
│ └── ...
├── common/ # 公共组件（日志、中间件、工具函数）
├── config/ # 配置文件
└── app.py # 入口文件（仅负责组装和启动）
```

## 二、解耦要求

1. **依赖倒置**：模块之间通过接口、抽象类或依赖注入交互，避免直接依赖具体实现。
2. **减少跨模块硬编码**：不在模块内部直接 import 另一个业务模块的具体实现类，应通过抽象层或事件/消息机制通信。
3. **独立可测试**：每个模块应能在不启动整个系统的情况下单独进行单元测试。
4. **配置外置**：数据库连接、API 密钥、环境变量等不写在代码中，统一放在配置文件或环境变量中。

## 三、易于定位与查看

1. **命名可读**：文件名和目录名清晰表达其用途（如 `user_service.py`、`payment_gateway.js`）。
2. **入口明确**：提供 `README.md` 说明目录结构和核心模块的职责。
3. **引用路径清晰**：使用相对路径或路径别名（如 `@/modules/user`），避免深层 `../../..`。
4. **代码注释**：每个文件头部简要说明该文件的作用；公共函数/类必须有 docstring 或注释。

## 四、易于修改

1. **局部影响**：修改一个业务需求时，理想情况下只需改动 1~2 个文件（例如仅修改对应的 service 文件）。
2. **开放-封闭原则**：对扩展开放，对修改封闭。新增功能应通过添加新文件/新模块实现，尽量不修改已有稳定代码。
3. **使用版本控制友好结构**：避免超大文件和频繁合并冲突的目录布局（例如平铺几十个不相关的文件）。

## 五、验收检查点

- [ ] 项目根目录下是否有模块化的子目录？
- [ ] 是否存在超过 400 行的单个代码文件？若有，请拆分。
- [ ] 任意两个模块之间是否存在循环依赖？
- [ ] 修改 A 模块的功能是否会导致 B 模块也需要修改？
- [ ] 新人阅读代码时，能否在 5 分钟内定位到“用户注册”逻辑所在的文件和函数？

请严格按照以上要求输出代码结构和具体实现。
